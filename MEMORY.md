# Armbot 视觉 SLAM + 导航 — 需求与流程

> 分支: `feature/SLAM2.0`
> 目标: 用相机做实时 3D 重建 + 自动导航避障，不依赖 LiDAR

---

## 一、项目背景

Armbot 是一个自主移动抓取机器人。当前阶段负责**视觉 SLAM + 导航**模块，小车需要通过相机感知环境并自动导航到目标点。

- **硬件**: RDK X5 (ARM64, ROS 2 Humble)、LeArm 机械臂、麦克纳姆底盘、USB 相机
- **GPU**: RTX 5060 (8GB, 用于模型推理)
- **仓库**: https://github.com/Tang29Zx/Armbot

---

## 二、核心需求

1. **实时 3D 重建**：用相机拍摄 RGB 图像，实时重建 3D 点云 + 相机轨迹
2. **自动导航**：给定目标点，小车自动规划路径并驱到底盘到达目标
3. **自动避障**：遇到障碍物自动绕行
4. **机械臂抓取**：到达目标后，YOLO 确认物体，VLA 生成动作，机械臂执行抓取

---

## 三、技术方案

```
┌─────────────────────────────────────────────────────┐
│                    相机 (RGB)                        │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│  lingbot-map (Geometric Context Transformer)         │
│  · 输入: RGB 图像序列                               │
│  · 输出: 3D 点云 + 相机位姿 (~20 FPS)               │
│  · 模型: ~4.4GB (已下载到 models/)                   │
└──────────────────────┬──────────────────────────────┘
                       │ 3D 点云 + 相机位姿
                       ▼
┌─────────────────────────────────────────────────────┐
│  lingbot_nav_bridge (待开发)                         │
│  · 3D 点云 → 地面投影 → 2D 占用栅格地图              │
│  · 相机位姿 → map → odom TF 变换                     │
│  · 发布 /map + /odom                                │
└──────────────────────┬──────────────────────────────┘
                       │ /map + TF + /odom
                       ▼
┌─────────────────────────────────────────────────────┐
│  Nav2 (navigation2)                                  │
│  · 全局路径规划 (planner)                            │
│  · 局部避障 (controller)                             │
│  · 发布 /cmd_vel → 底盘执行                          │
└──────────────────────┬──────────────────────────────┘
                       │ /cmd_vel
                       ▼
┌─────────────────────────────────────────────────────┐
│  chassis_control (I2C bus 5, addr 0x34)              │
│  · 麦克纳姆底盘驱动                                  │
│  · 编码器 → /odom                                    │
└─────────────────────────────────────────────────────┘
```

---

## 四、待开发模块

### 4.1 lingbot_nav_bridge

- **输入**: lingbot-map 输出的 3D 点云 + 相机位姿
- **输出**: Nav2 兼容的 `/map`、`/odom`、TF 变换
- **关键逻辑**: 3D 点云 → 地面投影 → 2D 占用栅格；位姿 → odom TF

### 4.2 Nav2 配置

- 全局代价地图 (global costmap)
- 局部代价地图 (local costmap)
- 规划器 + 控制器参数适配视觉 SLAM

---

## 五、文件结构

```
ros2_ws/src/
├── lingbot_map/                # 3D 重建模型 (submodule)
│   └── demo.py                 # 推理入口
├── lingbot_nav_bridge/         # 桥接节点 (待开发)
├── chassis_control/            # 底盘驱动 (已完成)
├── armbot_bringup/             # 启动文件
│   ├── launch/
│   │   ├── robot.launch.py     # LiDAR 版本 (SLAM1.0)
│   │   └── vision_nav.launch.py # 视觉导航版本 (待开发)
│   └── config/
│       └── nav2_vision_params.yaml  # Nav2 视觉配置 (待开发)
├── action_interfaces/          # 消息定义
└── action_pkg/                 # 机械臂控制
```

---

## 六、当前进度

| 模块 | 状态 |
|------|:--:|
| lingbot-map 模型下载 | ✅ 4.4GB (models/) |
| PyTorch + FlashInfer | ✅ 已安装 |
| lingbot nav bridge | ❌ 待开发 |
| Nav2 安装 | ❌ SD 卡坏块阻塞 |
| 底盘控制 | ✅ I2C 调通 |
| 机械臂 I2C | ✅ 0x30 已检测 |
| SLAM1.0 (LiDAR) | ✅ 建图成功 |

---

## 七、风险

- **Nav2 无法安装**: SD 卡坏块阻塞 `apt install`，需修复
- **lingbot-map 实时性**: 模型推理延迟能否满足导航实时性待验证
- **3D→2D 投影质量**: 点云密度、地面分割准确性影响导航
