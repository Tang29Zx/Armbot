# AGENTS.md

## 1. 适用范围与优先级

本文件定义仓库级工程协作规则，适用于代码、脚本、文档、测试、配置、部署和自动化任务。

优先级：

```text
安全与数据保护
> 明确的用户目标
> 项目内规则与契约
> 正确性和可验证性
> 可维护性
> 交付速度
```

基本目标：

- 解决真实问题。
- 保持实现简单、可靠、可维护。
- 留下可验证、可复现、可交接的项目资产。
- 精准修改，不扩展与当前目标无关的范围。

## 2. 沟通与文档

- 对话、解释、计划、review 和总结使用中文。
- 代码、注释、标识符、文件名、命令、环境变量和 PR 内容默认使用英文。
- README、技术文档、产品文案、验证记录、PRD 和 Tech Spec 默认使用中文。
- 结论先行，再解释原因、用户影响和关键取舍。
- 基于事实沟通；发现需求、方案或假设有问题时直接指出。
- 下一步明确且风险可控时直接执行；只有目标不清、权限不足、运行时缺失或存在真实风险时才暂停询问。
- 最终回复必须独立完整，不依赖中间过程消息。

## 3. 开始任务前

开始任何项目工作前：

1. 请读取 `~/.codex/AGENTS.md`
2. 确认仓库根目录和当前分支。
3. 检查 `git status`，保留用户已有修改，不覆盖无关改动。
4. 阅读适用的 `AGENTS.md`、README、配置、清单和相关代码。
5. 如果存在 `MEMORY.md`，读取其中长期有效的架构决策和运维备注。
6. 明确本轮目标、涉及范围、完成标准和验证方式。

除非任务明确要求，不创建额外流程文档，也不修改无关配置。

## 4. 工作流程

复杂任务采用：

```text
Specify -> Plan -> Task -> Execute -> Verify
```

复杂任务包括新功能、架构或 API 变更、数据模型变更、安全变更、部署变更和多文件重构。编码前应明确：

- 用户目标和验收标准。
- 风险、约束和非目标。
- 实现步骤和验证方式。
- 必要时的回滚方式。

简单任务保持轻量，但仍需聚焦修改并完成最小验证。

实现路径由 agent 负责选择。存在多条路径时，优先采用最短、可靠、可维护且符合现有项目模式的方案。

## 5. 实现原则

- 保持简单，优先修复根因。
- 不添加臆测性的功能、抽象、配置、依赖或扩展点。
- 优先复用现有 helper、utility、组件和项目约定。
- 不通过重写整个模块解决局部问题。
- 新增抽象前，应存在至少两个真实复用点或明确领域边界。
- 保持模块和函数小而清晰；避免不必要的 class 和共享状态。
- 在 API、文件、网络、用户输入等信任边界验证数据。
- 显式处理失败、空值、超时、重试和清理路径。
- 注释解释“为什么”，不重复代码已经清楚表达的行为。
- 外部 API 和第三方库行为应参考官方文档及项目锁定版本，不依赖猜测。
- 未经明确要求，不改变公共接口、默认配置或稳定运行链路。

## 6. 安全与数据保护

- 不读取、打印、复制或提交 `.env`、私钥、token、密码和凭据。
- 不在代码、文档、日志或 `MEMORY.md` 中保存 secret 值；只记录配置位置或变量名。
- 不硬编码凭据、生产地址或个人敏感信息。
- 验证和清理不可信输入，避免日志和错误信息泄露敏感数据。
- 处理认证、上传、外部请求、支付、AI 工具和用户输入时检查常见 OWASP 风险。
- 生产代码不使用 `eval()` 等不安全执行方式，除非有不可替代的受控场景和明确审查。
- 破坏性操作前说明影响范围、验证方式和回滚方案。
- 除非用户明确授权，不修改生产或外部系统状态。

## 7. 验证要求

汇报完成前，根据改动类型执行适当验证：

- 语法检查、格式检查、lint 和类型检查。
- 相关单元测试、集成测试或代表性运行命令。
- 修复 bug 时，尽可能先复现问题或建立失败用例。
- 使用代表性输入检查正常路径和关键失败路径。
- 服务或应用可本地运行时，实际启动并验证主要流程。
- UI 工作检查真实渲染、桌面端与移动端、溢出、遮挡、间距和关键交互。

验证失败时先修复并重新运行。不得把未验证或失败的结果描述为完成。

无法执行验证时，必须说明原因、影响和采用的替代检查。

## 8. 工具与文件操作

### 搜索

- 内容搜索优先使用 `rg`。
- 文件搜索优先使用 `fd`；不可用时使用 `rg --files`。
- 只读取完成任务所需的文件，避免无目的加载大量内容。

### 包管理

- 根据 lock file 和现有配置选择包管理器。
- Python 项目优先使用项目已有环境；新项目优先考虑 `uv`。
- GitHub 操作优先使用 `gh` 或已配置的 GitHub 集成。
- 不随意升级依赖或改写 lock file。

### 文件编辑

- 使用最小、聚焦的 patch。
- 不重写或格式化无关文件。
- 不删除用户未要求删除的内容。
- 不回滚用户已有改动，除非用户明确要求。
- dirty working tree 中保留无关修改，并在提交前检查实际 diff。

## 9. Git 规则

- 未经明确要求，不自动 commit、push、merge、rebase 或创建 PR。
- 提交前检查 `git status`、暂存区和 diff。
- commit message 可使用中文或英文；同一组提交尽量保持语言一致，并描述用户可见变化或修复根因。
- 不把生成物、日志、缓存、凭据或本地运行状态提交到仓库。
- 不使用 `git reset --hard`、`git clean`、force push 或破坏性 rebase，除非用户明确授权且已说明影响。
- 历史重写和 force push 优先使用备份分支及 `--force-with-lease`。
- 不以 “AI generated” 作为提交价值说明。

## 10. UI 与设计任务

涉及参考图、Figma、设计稿、视觉还原或交互原型时：

- 开始前明确目标平台、数据状态、交互范围和验收标准。
- 使用真实 assets、tokens 和可追踪的设计事实来源。
- 先完成静态保真和截图验证，再接入真实数据和进行组件化重构。
- 不把 PNG/JPEG 预览图当作最终可编辑交付物。
- 缺少关键设计上下文时，先指出缺口，不臆造产品规则。

## 11. Code Review

- 问题优先，按严重程度排序。
- 先说明用户影响，再说明原因和修改建议。
- 引用最小相关代码和准确文件路径。
- 区分真实缺陷、风险、测试缺口和格式偏好。
- 重点检查正确性、安全、数据损坏、兼容性、并发、性能和回归风险。
- 没有发现问题时也要说明仍未覆盖的验证范围。

## 12. 最终交付

最终回复应简洁说明：

- 做了什么。
- 修改了哪些文件或外部状态。
- 运行了哪些检查及其结果。
- 尚未验证的内容、限制和剩余风险。
- 是否更新了 `MEMORY.md`。

不要把计划、尝试或部分成功描述为最终完成。

## 13. 上下文压缩

压缩上下文时保留：

1. 当前目标和验收标准。
2. 架构决策和关键取舍。
3. 修改过的文件和验证结果。
4. 未完成事项、已知风险和失败路径。
5. 用户纠正及仍然有效的项目约束。
